import ContactClient from '@/components/ContactClient';

export const metadata = {
  title: 'Contact Us | WebTechAI',
  description: 'Get in touch with WebTechAI to discuss your next big idea. Secure a strategy session with our elite specialists.',
};

export default function ContactPage() {
  return <ContactClient />;
}
